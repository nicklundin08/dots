#!/usr/bin/env bash


pre-commit && \
	git add . && \
	cz commit && \
	hm.rebuild && \
	git push && \
	git switch $(hm.show_host_for_git) && \
	git merge --ff main && \
	git push && \
	git switch main
