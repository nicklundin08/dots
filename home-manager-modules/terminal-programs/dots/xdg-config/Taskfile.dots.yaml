version: "3"

vars:
  XDG_CONFIG_HOME: '{{default "~" .XDG_CONFIG_HOME}}'
  DOTS_DIR: '{{.XDG_CONFIG_HOME}}/DOTS'

      
includes:
  hm:
    taskfile: ./Taskfile.home-manager.yaml
  nixos:
    taskfile: ./Taskfile.nixos.yaml

