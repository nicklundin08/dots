#!/usr/bin/env bash


git switch $(hm.show_host_for_git) && \
	pre-commit && \
	git add . && \
	cz commit && \
	hm.switch && \
	git push && \
	git switch main && \
	git merge --ff $(hm.show_host_for_git) && \
	git push && \
	exec bash
