# TODOS
Misc
- main
    - archunit
    - gradle plugins for openapi generator
    - gradle plugins for jsonschem to pojo (events)
    - gradle plugins for liquibase
    - Dockerfile?
    - K8s manifest?
- tooling:openapi
    - Enforce nonbrekaing changes
    - Update taskfile to generate ts library correctly
- Liquibase
    - Liquibase user
    - Separate schemas
    - Schema standard users (user_ro, user_rw)
    - Core user
- Create some sample events with json schema and generate some core api event classes
