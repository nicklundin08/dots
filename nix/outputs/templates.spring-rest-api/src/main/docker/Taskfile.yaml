version: "3"
tasks:
  build:
    summary: |
      Builds the docker image for the spring boot server.
    desc: |
      Builds the docker image for the spring boot server.
    cmds:
      - podman build -f ./Dockerfile ../../ -t $DOCKER_IMAGE_TAG_NAME
